<svg version="1.1"
  baseProfile="full"
  width="{{.Width}}" height="{{.Height}}"
  xmlns="http://www.w3.org/2000/svg">

  {{range .Circles}}
  <circle cx="{{.X}}" cy="{{.Y}}" r="{{.Radius}}" fill="{{.Color}}" />
  {{end}}
  
</svg>