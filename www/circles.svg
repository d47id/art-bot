<svg version="1.1"
  baseProfile="full"
  width="100" height="100"
  xmlns="http://www.w3.org/2000/svg">

  {{range .}}
  <circle cx="{{.X}}" cy="{{.Y}}" r="{{.Radius}}" fill="{{.Color}}" />
  {{end}}
  
</svg>